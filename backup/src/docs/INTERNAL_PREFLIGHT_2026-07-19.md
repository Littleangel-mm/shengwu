# 开发侧交付预检（2026-07-19）

## 已执行检查

- 后端静态检查：`ruff check app tests scripts`，通过。
- 后端类型检查：`mypy app`，通过。
- 后端自动化测试：83 项通过，1 项数据库环境条件测试跳过。
- 数据库迁移：已从 `20260719_003` 升级到 `20260719_004 (head)`。
- 前端检查：Oxlint、Vitest（5 项）和生产构建均通过。
- 前端生产依赖审计：`npm audit --omit=dev`，0 个已知漏洞。
- PostgreSQL 备份：已生成 custom-format 备份并通过 `pg_restore --list` 完整性读取；
  验证文件随后删除，未保留客户数据副本。
- 并发冒烟：10 并发、100 次 readiness 请求，100 次成功。

## 已知限制与遗留风险

1. 当前 `research_app` 数据库角色没有 `CREATEDB` 权限，因此没有在本机自动创建隔离数据库执行
   完整恢复演练。生产上线前应由 DBA 按运维手册在隔离实例执行恢复并记录 RTO/RPO。
2. 当前共享 Python 解释器执行 `pip-audit` 报告 67 个漏洞条目，其中包含 LangChain、yt-dlp
   等非本项目依赖；项目依赖链也包含需关注的 Starlette、Pillow、lxml 等条目。上线前必须在
   干净虚拟环境重新锁定和审计依赖，按兼容性测试结果升级，不能把共享解释器结果当作项目
   的精确软件物料清单。
3. 前端主包约 501 kB，构建成功但存在拆包优化提示，不阻塞功能验收。
4. 客户真实样本准确率、正式 UAT、恢复演练、压力基线确认及三方签字尚未执行。

## 结论

开发侧功能、迁移、自动化测试、生产构建、备份可读性和基础并发冒烟已通过。正式生产发布
仍需完成上述外部待办，尤其是干净环境依赖审计、隔离恢复演练和客户 UAT。
